{
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd Backend && dotnet watch run --project Shneta.API/",
    "client": "cd Frontend && npm run dev"
  }
}
